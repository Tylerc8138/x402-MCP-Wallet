# CLAWD Wallet MCP Server Environment Variables

# Coinbase CDP API credentials (from https://portal.cdp.coinbase.com/)
# Get these by creating an API key in the CDP Portal
CDP_PROJECT_ID=your_project_id_here
CDP_API_KEY_NAME=organizations/your_org_id/apiKeys/your_key_id

# Private Key (PKCS8 format)
# NOTE: If your key starts with "-----BEGIN EC PRIVATE KEY-----", convert it:
# openssl pkcs8 -topk8 -nocrypt -in your_key.pem
CDP_API_KEY_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
your_pkcs8_private_key_here
-----END PRIVATE KEY-----"

# Backend URL (where the Python backend is running)
CLAWD_BACKEND_URL=http://localhost:8402

# API Key for authenticating with the backend
# Must match ONRAMP_API_KEY in backend/.env
CLAWD_BACKEND_API_KEY=ad72219c-e3f9-4b6a-4058bcc75907

# TAP Registry (optional)
# CLAWD_TAP_REGISTRY=https://tap-registry.visa.com/v1
CLAWD_TAP_MOCK_MODE=true

# Treasury private key for referral payouts (optional)
# CLAWD_TREASURY_PRIVATE_KEY=0x...
